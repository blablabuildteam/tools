# Copy to .env.local — use the SAME Vercel KV / Upstash credentials as blablabuild.com
# so AI Matrix sessions (e.g. adsomnia-workshop) keep working without data loss.

KV_REST_API_URL=
KV_REST_API_TOKEN=

# Optional: public URL for share links
NEXT_PUBLIC_APP_URL=http://localhost:3000
